@model MyWallet.Web.ViewModels.BankAccount.BankAccountViewModel

@section breadcrumb {
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="@Url.Action("Index")">Bank Accounts</a></li>
            <li class="breadcrumb-item active" aria-current="page">Delete</li>
        </ol>
    </nav>
}

<div class="row">
    <div class="col-md-4">
        @using (Html.BeginForm("Delete", "BankAccount", FormMethod.Post, new { @class = "user" }))
        {
            @Html.HiddenFor(b => b.Id)
            @Html.HiddenFor(b => b.OpeningBalance)
            <fieldset>
                <legend>Delete BankAccount</legend>
                <hr />
                <div class="form-group">
                    @Html.LabelFor(b => b.Name)
                </div>
                <div class="form-group">
                    @Html.TextBoxFor(b => b.Name, new { @class = "form-control", @readonly = true })
                </div>
                <button type="submit" class="btn btn-danger btn-icon-split">
                    <span class="icon text-white-50">
                        <i class="fas fa-trash"></i>
                    </span>
                    <span class="text">Delete</span>
                </button>
            </fieldset>
        }
    </div>
</div>



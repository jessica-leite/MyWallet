@model MyWallet.Web.ViewModels.Category.EditCategoryViewModel

<div class="row">
    <div class="col-md-4">
        @using (Html.BeginForm("Edit", "Category", FormMethod.Post, new { @class = "user" }))
        {
            @Html.HiddenFor(m => m.Id)

            <fieldset>
                <legend>Edit Category</legend>
                <hr />
                <div class="form-group">
                    @Html.LabelFor(c => c.Name)
                </div>
                <div class="form-group">
                    @Html.TextBoxFor(c => c.Name, new { @class = "form-control form-control-user" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(c => c.UserId)
                </div>
                <div class="form-group">
                    @Html.TextBoxFor(c => c.UserId, new { @class = "form-control form-control-user" })
                </div>
                <button type="submit" class="btn btn-warning btn-icon-split">
                    <span class="icon text-white-50">
                        <i class="fas fa-pencil-alt"></i>
                    </span>
                    <span class="text">Edit</span>
                </button>
            </fieldset>
        }
    </div>
</div>
